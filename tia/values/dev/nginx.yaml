replicaCount: 5

image:
  repository: nginx
  pullPolicy: IfNotPresent
  tag: ""

  # image:
  # repository: leonardtia/articles
  # pullPolicy: IfNotPresent
  # tag: "develop"

fullnameOverride: "nginx"

service:
  type: NodePort

nodeSelector: {}
